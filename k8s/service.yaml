apiVersion: v1
kind: Service
metadata:
  name: frontend-rpubkey
  namespace: web-pubkey
spec:
  selector:
    app: frontend-rpubkey
  ports:
  - name: metrics
    port: 80
    targetPort: 80 
    protocol: TCP
  type: LoadBalancer